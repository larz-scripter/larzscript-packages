# lz-color

ANSI terminal colors for Larzscript. `larzscript pkg install color`.

```
import "color" as c
print(c.green("ok"), c.red("error"), c.bold(c.cyan("title")))
```
Colors: red green yellow blue magenta cyan gray · styles: bold dim underline invert.
